#include "auth.h"

Auth::Auth()
{
    create();
}

void Auth::create()
{
   //QFile file(FILE_NAME);
   //QDataStream stream( &file );
   //file.open( QIODevice::WriteOnly );
   //file.close();
    //QDomDocument doc;
    //QDomElement root = doc.createElement(this->metaObject()->className());
    //QDomElement user = doc.createElement("user");
    //user.appendChild(doc.createTextNode("manager"));
    //root.appendChild(user);
    //doc.appendChild(root);
    //QFile output(FILE_NAME);
    //output.open( QIODevice::WriteOnly );
    //QTextStream stream(&output);
    //doc.save(stream, 2);
}
